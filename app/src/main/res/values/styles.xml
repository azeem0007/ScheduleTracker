<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme extending AppCompat with DarkActionBar -->
    <style name="Base.Theme.ScheduleTracker" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="android:windowBackground">@drawable/background</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Custom theme extending the base theme -->
    <style name="Theme.ScheduleTracker" parent="Base.Theme.ScheduleTracker">
        <!-- Additional customizations if needed -->
    </style>

    <!-- Define other styles here -->
    <style name="CustomTextStyle">
        <item name="android:textSize">24sp</item> <!-- Increase font size -->
        <item name="android:textColor">#000000</item> <!-- Black color -->
    </style>
</resources>
